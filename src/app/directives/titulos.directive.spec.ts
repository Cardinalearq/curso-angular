import { TituloSizeDirective } from './titulos.directive';

describe('TitulosDirective', () => {
  it('should create an instance', () => {
    const directive = new TituloSizeDirective();
    expect(directive).toBeTruthy();
  });
});
