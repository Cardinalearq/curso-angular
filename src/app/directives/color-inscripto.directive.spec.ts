import { ColorInscriptoDirective } from './color-inscripto.directive';

describe('ColorInscriptoDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorInscriptoDirective();
    expect(directive).toBeTruthy();
  });
});
